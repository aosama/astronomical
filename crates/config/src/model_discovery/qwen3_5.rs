//! Qwen3.5 discovery rules are kept separate from family classification.

// The family-specific helper module is intentionally small for now. Qwen's
// artifact completeness and vision rules remain in the discovery owner until
// another executable family needs to share those rules.
