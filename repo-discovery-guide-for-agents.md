# Repo Discovery Guide for Astronomical

Agent orientation map for non-obvious repository facts; this is not user-facing documentation or a changelog.

## Maintenance mandate

Read this guide before substantive work and spot-check 2-3 key facts. Update it when paths, entry points, conventions, or expensive gotchas change. Treat it as suspect when Last verified is more than 90 days old.

Last verified: 2026-08-07 (spot-checked: workspace membership and default members, production standard-safetensors paging, experimental aligned expert-pack isolation, public entry points, compact image IPC, supervisor and worker ownership, live MLX memory control, automatic sparse residency, cache-safe large prefill, terminal-only MTP prefill, bounded prompt-cache write-behind, model-serving source ownership, multi-family dispatch boundary, hidden DeepSeek readiness, and the embedded Observatory compact Overview panels)

## Project overview

Astronomical is an Apple Silicon local model server. The active architecture is a localhost Rust HTTP/SSE process plus one Rust worker subprocess; the worker owns MLX, artifact validation, tokenization, model state, and generation. Only one model is resident in GPU memory at a time; the supervisor hot-swaps to a different model when a request targets a different discovered model. Obsolete paths not needed for real Qwen3.5 OpenAI-compatible SSE have been removed.

## Known gotchas

- The local API supports POST /v1/chat/completions for OpenAI chat chunks and POST /v1/responses for Copilot-compatible response items; both translate directly into the same worker Generate command.
- Models are loaded lazily: menu-app startup launches an idle worker with no resident model. The supervisor discovers configured models for /v1/models, canonicalizes supported provider-prefixed IDs, then sends SwapModel only when the first (or a different) REST generation request targets a discovered model. It waits for ModelSwapped before forwarding generation. A rejected model load leaves the worker idle or preserves the previous model so a later valid request can proceed.
- Generation requests are processed serially (one at a time) with a bounded FIFO queue. Up to GENERATION_QUEUE_DEPTH requests can wait while one is active; requests beyond the queue depth receive HTTP 429 with server_capacity. Queue permits are released immediately when a queued request is cancelled or its HTTP client disconnects.
- XPC code and packaging are deleted. The daemon discovers its sibling inference worker automatically.
- The supervisor must not depend on model-serving or runtime-integration; MLX belongs only in the worker.
- IPC uses one length-delimited JSON frame per command or event with a 32 MiB cap matching HTTP chat transport. Decoded image files remain byte vectors in memory but use base64 strings on the JSON wire so an HTTP-accepted image does not expand into a JSON integer array. An oversized Generate command returns HTTP 413 with request_too_large and leaves the loaded worker healthy. There are no fragments, envelopes, protocol versions, sessions, public message/tool count caps, or smaller REST/IPC field byte caps; keep semantic validation and schema-depth checks instead.
- Chat text validation has no smaller per-message or aggregate-content cap in REST or IPC. The HTTP chat body has a 32 MiB transport cap.
- Qwen3.5 prompt admission, execution, and sampling read vocabulary and context limits from validated config. Initial admission reserves context-growing full-attention key/value state and the model-derived largest complete expert-layer page; there is no fixed expert-paging headroom field. After an allocation-free persistent prompt-cache lookup, admission also reserves one restored-prefix key/value payload because reconstruction temporarily owns both loaded blocks and concatenated live state. Restoration drops loaded block owners before allocator cleanup, then re-admits only the context remaining after the restored prefix; failed restoration resets request state, clears allocator memory, and repeats cold-context admission. Per-forward admission projects exact persistent growth against the configured ceiling and exact-context transient evidence against the one-percent allowed peak. A second same-context transient window only freezes expert-retention growth.
- Current OpenCode MCP schemas use both type: ["string", "null"] and two-branch nullable anyOf, including constrained integer branches. Copilot CLI also uses two-branch non-null anyOf such as string-or-array paths, plus interactive canvas tools whose input property intentionally has no type; the Qwen3.5 output parser dynamically parses those supported unions and truly untyped properties. Thinking-enabled generation begins after a prompt-owned <think> prefix, so its parser must start in reasoning state rather than waiting for the model to repeat that marker.
- Copilot CLI 1.0.73 discards text from valid bring-your-own-key response.reasoning_text.delta events and records only an opaque reasoning item ID, producing blank reasoning entries in its interface. For practical Copilot and industry compatibility, Astronomical exposes raw Qwen3.5 reasoning as plaintext summary_text through response.reasoning_summary_text.delta/done and completed reasoning summaries. It never emits plaintext as encrypted_content.
- Public stream failures retain four stable, bounded codes: chat_invalid_request, chat_engine_busy, chat_malformed_model_output, and chat_worker_unavailable. A pre-stream model rejection returns model_load_failed with the worker's cause chain instead of misreporting whole-worker unavailability. Keep human messages actionable without exposing native errors or local paths.
- OpenAI completion IDs carry a process-instance namespace before the request counter so tool-call IDs remain distinct across daemon restarts. Historical tool-call IDs are lifecycle-scoped: reuse is valid only after the earlier call has a result, while unresolved duplicates still fail validation.
- ProtocolWriter::close(self) intentionally consumes the writer so dropping owned child stdin delivers EOF on Unix.
- InferenceEngineError::InvalidRequest is request-scoped at generation start, token advancement, and injected-token advancement: emit WorkerEvent::Failed and keep the loaded worker reusable. Truly fatal worker returns use a bounded Tokio runtime shutdown because Tokio standard-input reads cannot be cancelled and must not leave a half-dead process retaining MLX memory.
- Normal worker builds include direct-mlx. The supervisor resolves user configuration and sends worker-owned startup paths over typed IPC. The inference worker starts without a model directory and receives a selected directory only through SwapModel.
- ModelFamily classifies Qwen3.5 and DeepSeek-V4 model types, but discovery returns only executable Qwen families. DeepSeek-V4 is intentionally absent from /v1/models and the worker factory rejects it before replacing a ready Qwen model.
- model_family_runtime owns the only concrete family enum boundary. inference_engine, engine_backed_worker, IPC, REST, and runtime-integration remain architecture-neutral; the worker selects a family once during model creation.
- model_directories entries in the standard Astronomical configuration are recursive scan roots, not guaranteed leaf model directories. Ignored model qualification and measurement tests must resolve an exact model ID through Astronomical discovery and must not fall back to an external model-manager directory.
- Qwen3_5MoEArtifactValidator discovers executable model shards and optional vision files from the index, accepts missing MTP-only files for target-only serving, validates optional OptiQ metadata when present, ignores unrelated files, and does not full-hash model payloads.
- Qwen3.5 artifact storage dtype is independent from its configured activation dtype. Config-derived model and vision tensor profiles accept and retain MLX-supported FP16, BF16, or FP32 storage, while affine scales and biases accept the same three MLX-supported floating dtypes. Sparse expert plans preserve each source dtype without artifact-name exceptions or conversion.
- Dense model_type qwen3_5 checkpoints use the dedicated qwen3_5 public boundary and execute dense SwiGLU without router or expert allocation. Shallow discovery uses indexed physical visual weights, while loaded-worker image capability requires a complete validated embedded or separate vision tower; an embedded tower may share language shards or occupy a vision-only model shard. Metadata alone remains text-only.
- OptiQ metadata accepts the same affine widths as MLX (2, 3, 4, 5, 6, and 8 bits). Embedding and output-head measurements are optional, but must match config when present.
- The worker process RSS sampler, hardcoded launch-memory envelope, and heartbeat watchdog layers are gone. Model loading has a 60-second timeout; cancellation acknowledgement has a separate 60-second bound because one large MLX prefill chunk can legitimately outlive the 3-second process-shutdown grace period.
- The inference worker resolves one positive MLX wired-memory ceiling at startup. A positive iogpu.wired_limit_mb value is used directly; zero resolves the macOS default once through MLX device information. That value is the only memory ceiling for MLX limits, request admission, and expert paging. maximum_mlx_memory_gb is an optional decimal-SI user ceiling in config.json. A live change uses PUT /v1/config/maximum-mlx-memory; an active generation queues only the newest valid cap and applies it before the next request. Status exposes machine, effective, minimum, configured, pending, and error state. Request completion and cancellation clear reclaimable allocator state. Replacement-model loading also clears the process-global allocator pool after the prior model is dropped and before replacement weights load. Missing or malformed sysctl output fails startup. Hourly file diagnostics use a 1,024-line lossy queue so slow storage cannot grow memory or stall inference.
- The macOS menu keeps successful maximum-model-RAM feedback visible until status confirms the requested effective ceiling with no pending update, then clears it after one second. Queued applications remain visible until they take effect, and late worker rejections become failure feedback.
- The explicit scripts/test-mlx-memory-contracts.sh layer runs native MLX allocator contracts, direct Rust MLX-C contracts, and the required Qwen3.6-35B-A3B-8bit to Qwen3.6-35B-A3B-oQ4e-mtp lifecycle in serial isolated processes. It waits five seconds for Metal after model teardown before native allocation checks. The pinned native allocator rejects any new, reused, or host-backed allocation that would cross the configured ceiling plus one percent without changing active or peak counters. MLX allocator policy is process-global, so do not combine its tests with parallel GPU qualification. Completed model load can retain tiny graph bookkeeping, so compare cache relationally there; request-finalization cleanup must report zero cache with live model residency.
- Per-prefill-chunk allocator cleanup must synchronize the model-owned MLX graphics-processor stream first. The Metal allocator releases cached buffers without synchronizing submitted evaluation or direct Metal input/output work.
- The native MLX build uses runtime Metal kernel compilation so capable Apple GPUs receive Metal 4 matrix kernels; first-use compilation is cold-start work and later processes reuse the system cache.
- macOS can reject a linker-signed nested bundle executable with an invalid code-signature page even when static bundle verification passes. The app assembler must explicitly ad-hoc sign every Contents/MacOS executable before signing the enclosing bundle.
- The SwiftPM menu executable must enter through a SwiftUI App with @NSApplicationDelegateAdaptor. Marking the NSApplicationDelegate class itself as @main compiles and leaves a process running, but its launch callbacks do not run in the packaged app, so no status item or owned daemon appears. Keep regular activation until the status item registers, then switch to accessory mode on the next main-loop turn.
- UI values labeled GB use decimal SI units (1 GB = 1,000,000,000 bytes). Binary values must use the explicit GiB label; do not use ByteCountFormatter.CountStyle.memory, which applies binary scaling while producing labels such as GB.
- prefill-chunck-size-optimizer is an internal model-serving package for ask/tell one-dimensional online chunk tuning. Fixed mode requires positive fixed_prefill_chunck_tokens whether the optimizer setting is false or omitted. Explicit optimized mode derives 128-token doubling candidates through the validated model maximum, including that exact maximum when it is not a power of two, persists observations by context bucket, and excludes candidates larger than the remaining prompt from each decision. Affine model weights accept the MLX-supported 2-, 3-, 4-, 5-, 6-, and 8-bit widths with group sizes 32, 64, or 128. The pinned MLX split-K patch caps row-count influence after 64 row tiles so equivalent 2,048-token and 4,096-token quantized projections retain one reduction topology. Mixture-of-experts assignments are sorted at 64 or more assignments before gathered quantized matrix multiplication. A custom Metal weighted-sum kernel consumes sorted outputs directly instead of restoring the expanded token/top-k tensor.
- Cumulative prefill episode planning evaluates reachable states iteratively in increasing remaining-token order. Do not restore recursive per-transition planning: persisted terminal-tail observations can advance by one token, making call-stack depth proportional to a long prompt and aborting the worker.
- The model retains shapeless MLX-C compilations for mixture-of-experts SwiGLU plus multi-token prefill-only attention gating, precise gated normalization, gated-delta decay, and sparse/shared expert combination. Shapeless callbacks must use scalar broadcast constants rather than input-shaped constants, which freeze the first traced token shape. One-token decode deliberately keeps the prior paths. The sorted-expert weighted-sum and gated-delta Metal kernel owners are retained with the model. mlx_closure_new() returning a null context is the valid pre-compilation output placeholder, not an allocation failure.
- Sparse mixture-of-experts residency is always automatic. Every sparse model owns ExpertPager-backed selected-expert weights, while maximum_mlx_memory_gb is the only user memory control. Startup prewarms complete layers only while they fit the live ceiling. Pressure evicts partial pages before complete layers, synchronizes the graphics-processor stream, clears released allocator storage, and resamples before reducing prompt-processing work. Resident and paged are observed outcomes, not configuration modes.
- The worker reports expert residency transitions independently from generated output. /v1/status retains expert_memory_mode as resident, paged, or null. There is no expert-storage format field because production has one standard safetensors path. The menu flashes only actual resident/paged transitions.
- The menu derives cumulative session prompt reuse from completed-request prompt and reused-token totals; those totals survive model swaps and reset with the supervisor process. Its stacked MLX memory snapshot reconciles tracked expert, model-core, and request-context payload against measured active memory, assigns the remainder to runtime work, and derives available bytes from the machine-specific MLX ceiling. MLX has no allocator ownership tags, so never present the tracked categories as independently measured physical allocations.
- Do not replace bounded expert reads with typed memory-mapped MLX arrays without validating source alignment. Qwen3.6 packed UInt32 expert payloads can start at only two-byte alignment, and a GPU operation over such a typed view reads the wrong bytes; the bounded reader intentionally materializes aligned MLX storage.
- Bounded expert-reader cursor operations remain mutex-protected, but positional reads use immutable interval mappings and independent file offsets. The generic descriptor reader deliberately serializes same-reader positional reads because representative warm model loading regressed under concurrent host reads. The pinned MLX generic input/output pool uses half the logical processors clamped to 4–8 workers; keep this as one small native patch rather than adding a Rust scheduler or configuration layer.
- Production ExpertPager always constructs lazy bounded reads from standard safetensors and never discovers .astronomical-aligned-expert-packs, even when generated research files remain beside a model.
- experimental/aligned-expert-packs owns the research format, explicit preparer, feature-gated Metal input/output loader, hermetic contracts, native contracts, and three ignored one-layer data-plane qualifications. Its measurements cover an isolated load-to-gather-quantized-matrix-multiplication boundary and are not production-serving evidence.
- Decode keeps sampling on the graphics processor and submits one token ahead with mlx_async_eval. Forward evaluation includes the sampled output and independently branching convolution state; full-attention storage and fused recurrent sibling output are already reachable graph roots. Full-attention key-value storage grows in fixed 256-token steps; do not restore per-token host sampling or key-value concatenation.
- Real process/MLX/HTTP Qwen3.5 qualification is isolated behind the model-artifact-qualification inference-worker feature and an ignored test; normal workspace tests neither compile nor run that qualification module.
- The performance-measurement feature gates the model-artifact measurements and model_persistent_prompt_cache_warmup ignored tests. The artifact measurement covers cold throughput; the warmup test launches two worker processes sharing one SSD prompt cache and asserts the second restores cached blocks.
- Four in-process prefill_chunck_tokens sweep tests (1024/2048/4096/8192) share the same feature gate and are #[ignore]'d; they inject a Qwen3_5MoEPrefillChunckSizer through run_configured_worker_with_prefill_chunck_sizer_override over duplex pipes rather than spawning a subprocess, so they do not report peak physical footprint.
- Persistent Qwen3.5 prompt caching defaults to enabled and is SSD-backed under the standard Astronomical configuration directory with a default 50 GB global ceiling across every model and revision. persistent_prompt_cache_enabled disables both reads and writes when false; prompt_cache_max_size_gb controls decimal capacity when enabled. Each model retains an isolated model-and-revision subdirectory via PromptCacheConfig::for_model(). All SSD-backed cache implementation lives under crates/model-serving/src/persistent_cache/: state files, projected visual embeddings, safetensors validation, disk writes and reads, scanning, quota, indexing, retention, and counters. Qwen owns only typed layout construction, state conversion, image planning, model execution, and engine decisions. Cache contents are disposable local artifacts with no backward compatibility or migration support; bump the prompt-cache format version when serialized model-state semantics change, and clear the cache root when immediate disk reclamation is needed. PersistentPromptCacheDiskStore stores 2,048-token sequence-state files under kv_blocks/, GatedDeltaNet boundary snapshots under recurrent_snapshots/, and final BF16 projected image embeddings. Global eviction scans without following symlinks and deletes oldest-written files first, using the full path as a deterministic tie-breaker. File cleanup deletes or confirms absence before untracking; other deletion failures remain tracked and return a typed error. Snapshot-aware lookup restores only to a recurrent-consistent boundary. Failures fall back to cold prefill.
- Persistent prompt-cache captures serialize on the sole Machine Learning framework for Apple silicon owner thread, then transfer only ordinary bytes through a zero-capacity rendezvous to the filesystem writer under a 256 MB pending-byte ceiling. Model forwards are not clipped to the 2,048-token disk block size: one large forward captures exact intermediate convolution and gated-delta state, slices full-attention blocks afterward, and persists boundaries in parent order. Queue readiness compares one sequential block with pending memory and all crossed blocks with disk quota; exact serialized bytes remain authoritative. The rendezvous bounds ownership to the writer's current block plus one producer-owned successor, so later boundaries wait for parent ownership to advance instead of being rejected by transient in-flight bytes. Once any boundary is skipped or rejected, capture stops for that request so later orphan blocks cannot reclaim a usable recurrent snapshot. Under the serialized write lock, each publication revalidates and recreates the active model directory tree so external cache clearing does not disable later captures. The writer remains uncapped, preserves atomic publication, and abandons in-flight optimization work after a bounded shutdown wait.
- Adaptive RAM growth guarding is always enabled in production configuration. Direct engine construction may still disable it for controlled tests and benchmarks.
- performance_attribution_enabled defaults to false and writes worker-local aggregate model-loading and generation reports to ~/.astronomical/logs/performance-attribution.jsonl when enabled. The disabled path has no timing or writer allocation; changing it requires worker replacement. The enabled writer reopens a missing or replaced log path at the next report, so external rotation does not require an application restart. Report timings distinguish graph construction, asynchronous submission, synchronization waits, complete forward memory snapshots, and outer prefill, decode, attention, and multilayer-perceptron spans. Outer spans are excluded from the detailed attributed-duration sum because they overlap leaf operations. Reports include positional file-read calls, successful bytes, summed host read time, maximum concurrency, maximum single-read latency, and failures for attributed generic and bounded readers. These counters do not prove physical storage service because the operating-system page cache may satisfy reads.
- mtp_enabled is an explicit true-by-default user config field so supported speculative decoding activates automatically. Changing it requires worker replacement because runtime state depends on the loaded artifact. Supervisor status exposes both the preference and mtp_runtime_state. Valid artifacts with no, incomplete, or unsupported MTP inventory report target_only, while compatible depth-one MTP weights report active after binding. The native MTP decode path is depth-one and greedy-only: it admits exact rounded MTP plus target growth before drafting, verifies current plus draft in one target window, queues an accepted draft already present in target state, and keeps sampled requests target-only so MTP proposal randomness cannot perturb output distribution. Rejected verification retains the valid first target row through an architecture-neutral boundary: hybrid layouts restore captured convolution and recurrent tensors plus the attention frontier, while attention-only layouts restore an empty recurrent snapshot and truncate attention only. Do not infer this setting or runtime state from sidecar presence, model names, shard counts, or local artifact paths.
- Eligible text-only MTP requests run nonterminal prompt chunks through the exact target path without retaining hidden rows or growing private MTP history. The terminal prefill chunk alone captures target hidden rows and initializes private MTP history; the reserved final prompt token is still forwarded once at generation startup as the first draft seed. Terminal capture timing is attributed but excluded from chunk-optimizer learning, while a distinct prefix context prevents old full-history evidence from matching the cheaper path. Requests remain target-only whenever the persistent prompt cache is available because its snapshots do not contain shifted MTP history.
- Automatic worker replacement/backoff/restart-budget policy is gone. On a worker fault, the supervisor terminates the worker and reports plain unavailable; public health has only loading, ready, and unavailable.
- Old split-out crate skeletons crates/supervisor-control, crates/native-bridge, and crates/model-artifacts are gone; do not resurrect them for the active path.
- Native archive bootstrap still SHA-256-verifies downloaded MLX source archives; that is separate from model-payload startup validation.
- Native patches pass through crates/runtime-integration/native/apply_patch_if_needed.cmake, which records a content-hash marker after clean application and rejects partial/conflicting state. When migrating an older generated tree without markers or changing a patch in place, remove only its generated mlx-c-runtime-build directory and rebuild from the verified archive. Do not suppress patch failures.

## Conventions

- Follow .github/instructions/rust.instructions.md for Rust and keep code Java-readable.
- Use CARGO_BUILD_JOBS="$(sysctl -n hw.logicalcpu)" for non-trivial Cargo verification on macOS.
- Classify tests by the highest external dependency needed to prove their contract: hermetic, rest_api, direct_mlx, mlx_memory_contract, model_artifact_qualification, persistent_prompt_cache_qualification, performance_measurement, macos_menu_contract, native_metal_contract, or structural_guard. Do not create a new test binary only to express a semantic category; preserve the existing execution boundary when it is the more expensive dependency.
- Run Rust tests through Cargo aliases except scripts/test-direct-mlx.sh and scripts/test-mlx-memory-contracts.sh, which run isolated MLX targets with live progress and individual 120-second guards. Those scripts and commit verification pass Cargo verbose output so the active sccache rustc command remains visible during long compilation.
- Cargo aliases select hermetic, REST API, model-artifact qualification, persistent prompt-cache qualification, performance measurement, and parity targets by execution boundary. Slow and ignored lanes remain explicit.
- Keep production Rust sources at or below the 500-line principle; move a cohesive owner to a direct sibling instead of enlarging a near-limit file.
- The tracked Git pre-commit hook checks staged human-authored Rust, Python, shell, Swift, C-family, Metal, JavaScript, and TypeScript source including tests. It blocks files above 600 lines, reads the index rather than the worktree, and exempts only an explicit first-line generated marker. Run scripts/install-git-hooks.sh after cloning to activate it. The separate scripts/check-source-size.sh production-Rust scan warns at 501–550 lines and fails at 551+; run scripts/test-source-size-check.sh to verify both guards.
- Do not add crates, facades, registries, launchers, managers, actors, adapters, or services unless they remove code from the active path.
- Open Brain provenance is project-scoped: every Astronomical recall and structured memory write must declare project_id as astronomical. Store only durable decisions, verified evidence, explicit user preferences, unresolved blockers, and outcome summaries; do not store file inventories, transient test output, local-machine details, or completed implementation logs. Project-scoped recall can include unscoped legacy thoughts, so use only records whose returned project ID is astronomical unless separately verified. Before broad memory cleanup, confirm the candidate records belong to Astronomical.
- Markdown docs should not hard-wrap prose.

## Structure map

- docs/north-star-product-vision.md - the retained product direction; the former architecture, organization, and safety mandate documents were intentionally deleted because they encouraged framework growth.
- README.md, CONTRIBUTING.md, and SECURITY.md - public project, contribution, and vulnerability-reporting entry points. Keep private local docs and agent instructions ignored.
- .github/ - public issue and pull-request templates plus SHA-pinned workflows. CI first classifies changed paths in a lightweight job; static-only site, documentation, and public-asset changes skip macOS Rust/native verification while code changes run the capped verified native provisioning plus bounded Rust hermetic-target compilation and tests. The separate Pages workflow publishes site unchanged without adding a frontend build tool.
- site/ - dependency-free GitHub Pages source. The homepage and evidence reports share direct CSS and an SVG mark; each public claim distinguishes measured qualification, architecture facts, and unresolved performance questions.
- docs/expert-paging-cpu-gpu-ssd-execution-flow.md - critical verified map of host control, MLX graphics-processor execution, expert-weight memory-cache least-recently-used policy, and bounded safetensors SSD reads. Update it whenever those boundaries change.
- apps/supervisor/ - localhost astronomicald, Axum REST/SSE, one WorkerHandle, one concrete worker loop, and one WorkerProcess. It has no XPC, actor framework, lifecycle object, or session/launcher hierarchy. Each completed generation appends one JSONL line to ~/.astronomical/logs/performance.jsonl with per-request throughput (prefill_tok_per_second, generation_tok_per_second), latency, token counts, MLX memory, and model identity. The Astronomical Observatory embedded web admin console is served at root / on the same port as the OpenAI API; seven static assets (index.html, console.js, overview-compact.js, memory-control.js, optimizer.js, playground.js, console.css) under apps/supervisor/console/ are compiled into the daemon from src/console_assets.rs and merged into every Router builder through console_routes(). No new crate, process, or runtime dependency. GET /v1/system/telemetry exposes local GPU utilization sampled from IOKit AGXAccelerator PerformanceStatistics and nullable macOS memory pressure sampled through a bounded sysctl probe in src/system_telemetry.rs; null pressure means unavailable. That module owns the supervisor crate's only unsafe FFI and the crate-level unsafe lint remains denied everywhere else.
- apps/inference-worker/ - private worker executable; owns model startup and direct MLX serving.
- apps/astronomical-menu/ - standalone macOS 26 Swift Package menu-bar application. It owns a bundled astronomicald only when no healthy local supervisor exists, consumes the enriched /v1/status document, and renders the Astronomical-specific Orbital Telemetry popover. Build/test it with Swift Package Manager, not Cargo.
- crates/config/ - strict standard-user configuration loader used by the daemon and supervisor resolver; workers receive resolved startup settings through IPC.
- crates/rest-contract/ - public OpenAI-compatible REST/SSE DTOs.
- crates/ipc-protocol/ - bounded supervisor/worker command and event DTOs.
- crates/model-serving/ - artifact_validation/ owns shared required-file validation, bounded model-shard validation, artifact profile types, errors, and validated file descriptors. safetensors/ owns shared bounded framing and raw JSON-header parsing used by artifact validation, persistent prompt storage, and expert manifest parsing. expert_paging/ owns family-neutral expert storage geometry, bounded loading, memory budgets, retention, eviction, and cache accounting. memory/ owns architecture-neutral adaptive MLX memory admission evidence and MLX memory telemetry. engine_backed_worker/ owns worker construction, protocol, generation, output, failure, swapping, and memory-limit orchestration. model_family_runtime/ owns the closed processor, request, output, and engine enum boundary. The crate also owns tokenizer and prompt processing, the prefill chunk-size optimizer, inference_engine/, decoder_cache/, and persistent_cache/. Qwen source is split by ownership: qwen3_5/ owns model-specific validation and execution; qwen3_5_moe/ owns Qwen sparse tensor semantics, page assembly, routing, and the concrete Qwen3_5ExpertPager.
- crates/runtime-integration/ - only unsafe/native MLX-C production boundary, compiled closure ownership, and ahead-of-time Metal library validation. Its experimental aligned loader is compiled only by the experimental-aligned-expert-packs feature.
- experimental/aligned-expert-packs/ - non-production research package. It is a workspace member but not a default member and is never bundled in Astronomical.app.
- Integration test roots group by execution boundary: hermetic_tests.rs, rest_api_tests.rs, direct_mlx_tests.rs, mlx_memory_contract_tests.rs, qwen3_5_persistent_prompt_cache_qualification_tests.rs, model_artifact_qualification_tests.rs, and performance_measurement_tests.rs. The experiment owns its corresponding hermetic, direct-MLX, model-artifact, and native contracts.
- registry/tested_models.json - tested model identities with revision hashes and qualification notes.
- third-party/ - native dependency manifest, MLX source patches, version pins, public native/source notices, cargo-about configuration and template, and the generated locked Rust dependency notices. The macOS app builder bundles LICENSE, THIRD_PARTY_NOTICES, and RUST_DEPENDENCY_NOTICES.

## Entry points

- Inspect the machine GPU wired-memory ceiling: sysctl -n iogpu.wired_limit_mb.
- Format: cargo fmt --all.
- Fast hermetic tests: cargo test-hermetic.
- Public REST/API tests: cargo test-rest-api.
- Direct MLX contracts: scripts/test-direct-mlx.sh.
- Explicit MLX memory-contract qualification: scripts/test-mlx-memory-contracts.sh (requires both named local checkpoints and fails if either is absent).
- Production Rust source-size guard: scripts/check-source-size.sh; structural guard test: scripts/test-source-size-check.sh.
- Install the tracked staged-source pre-commit guard: scripts/install-git-hooks.sh.
- Model-artifact qualification: configure model_directories in the standard Astronomical configuration file, then run cargo qualify-model-artifacts.
- Deployment REST liveness: cargo qualify-deployed-model-rest-liveness runs one release-model worker through four public TCP requests on one process: long Chat with representative tools, short Chat, reused long Chat, then reused long Responses. It requires clean SSE completion and a ready worker afterward.
- Performance measurement support: cargo test-performance-measurement-support; slow model-artifact measurements run one command at a time with cargo measure-model-artifact-summary, cargo measure-persistent-prompt-cache-warmup, or cargo measure-model-artifact-prefill-2048.
- Qwen3.5 persistent prompt-cache qualification runs through cargo qualify-qwen3-5-persistent-prompt-cache. Its selected-size cell uses ASTRONOMICAL_PROMPT_CACHE_QUALIFICATION_PREFILL_CHUNCK_TOKENS set to 2048, 4096, or 8192. The ignored performance_measurement_tests optimizer-candidate observation exercises the real prefill_chunck_size_optimizer_enabled configuration; reuse ASTRONOMICAL_BENCHMARK_WORKER_HOME and set ASTRONOMICAL_REQUIRE_PREFILL_OPTIMIZER_STATE_RELOAD for the second process when verifying persisted optimizer reload.
- Pinned-Ornith cache interaction qualification runs through scripts/qualify-qwen3-5-persistent-prompt-cache-interactions.sh. It executes fixed and optimized prefill across same-process reuse, process restart with pre-existing files, and deletion while the engine remains live; each cell has its own 120-second process timeout.
- Qwen3.6 automatic cold/warm attribution benchmark: cargo measure-model-artifact-performance-attribution; it is ignored by default, uses an internal 120-second timeout, and skips when the exact local checkpoint is absent.
- Experimental one-layer measurements: cargo measure-experimental-aligned-expert-packs-ornith-generation, cargo measure-experimental-aligned-expert-packs-ornith-prompt-processing, and cargo measure-experimental-aligned-expert-packs-oq4e-data-plane. These require explicit GPU approval before execution.
- Inspect one already-downloaded research model without mutation: cargo run -p astronomical-experimental-aligned-expert-packs --bin astronomical-experimental-aligned-expert-pack-preparer -- --model-directory PATH --dry-run.
- Experimental native Metal input/output contracts: experimental/aligned-expert-packs/scripts/test-experimental-native-metal-loader.sh contracts. This requires explicit GPU approval before execution.
- Before a requested commit or push, run scripts/verify-before-commit.sh for bounded formatting plus filtered hermetic and REST API verification; never substitute the broad serial Cargo all-target test command.
- Regenerate locked Rust dependency notices with scripts/generate-rust-dependency-notices.sh. Use its --check mode to verify the checked-in artifact is current.
- Build real binaries: CARGO_BUILD_JOBS="$(sysctl -n hw.logicalcpu)" cargo build -p astronomical-inference-worker --bin astronomical-inference-worker -p astronomical-supervisor --bin astronomicald.
- macOS menu contract tests: scripts/test-macos-menu-contracts.sh runs SwiftPM with live progress and a 120-second process timeout.
- Build and assemble the macOS menu bar app: scripts/make-astronomical-app.sh builds Rust daemon/worker binaries plus the Swift menu package, then produces target/astronomical-macos-release/Astronomical.app.
- Preview the public site with python3 -m http.server --directory site. Pushes to main that change site or the Pages workflow publish the site through GitHub Actions.
- Run real Qwen3.5 daemon: put model directories in ~/.astronomical/config.json, then run CARGO_BUILD_JOBS="$(sysctl -n hw.logicalcpu)" cargo build -p astronomical-inference-worker --bin astronomical-inference-worker -p astronomical-supervisor --bin astronomicald && cargo run -p astronomical-supervisor --bin astronomicald.
- Open the Observatory web console: run astronomicald, then open http://localhost:<bound-port>/ in a browser. The console polls /v1/status, /v1/cache/stats, and /v1/models every second and streams chat through /v1/chat/completions. No separate build step or frontend process.
- Model directories and prompt-cache capacity come from the standard Astronomical configuration file. Test workers use temporary configuration homes and typed IPC startup configuration instead of shell launchers.

## What to verify

- Whether the active OpenAI SSE path still streams real Qwen3.5 output.
- Whether docs or code reintroduced alternate transports, periodic RSS/heartbeat policy, automatic replacement/backoff, or multi-layer worker management.
- Whether REST/SSE DTOs and IPC DTOs remain bounded and separate, with image bytes compact on the Generate wire and oversized requests remaining non-fatal to the worker.
- Whether the family runtime enum delegates Qwen3.5 chat preparation without changing Qwen3_5InferenceRequest behavior and worker startup constructs one EngineBackedWorker directly; do not restore pass-through prepared-chat or configured-worker wrappers.
- Whether DeepSeek-V4 remains a typed unavailable stub only, excluded from discovery and the models endpoint until its real artifact implementation is intentionally onboarded.
- Whether expert_paging remains free of concrete Qwen and DeepSeek tensor semantics while qwen3_5_moe retains only Qwen layer plans, quantization interpretation, page assembly, routing, and pager coordination.
- Whether model-serving still uses bounded small-file captures and safetensors header checks instead of full model-payload hashing.
- Whether the inference worker still derives MLX memory ceilings from iogpu.wired_limit_mb without a literal production memory budget.
- Whether request, forward-growth, and expert-retention admission use only the startup-resolved MLX ceiling and local MLX counters.
- Whether the metrics E2E still reports token counts, timing-derived throughputs, and Apple physical footprint without joining regular test runs.
- Whether runtime Metal kernel selection, sorted large mixture-of-experts batches, and compiled native-dtype SwiGLU remain enabled.
- Whether persistent prompt-cache KV files remain exact 2,048-token SSD blocks, recurrent snapshots and visual embeddings remain separate, and exact-boundary prompts avoid non-block-aligned recurrent-state restores.
- Whether cache-enabled 4,096-token and 8,192-token selections remain one complete model forward, produce ordered 2,048-token child blocks when the writer is ready, and remain unclipped when the writer is unavailable.
- Whether user config remains strict, tiny, env-overridable, and limited to runtime preferences.
- Whether unsafe FFI remains confined to runtime-integration, astronomical-menu, and the supervisor system telemetry module, while only the worker links MLX.

## Maintenance snapshot

Verified workspace default membership, standard-safetensors-only production expert paging, ignored generated experimental files, experimental package and native feature isolation, Cargo test aliases, compact image IPC, non-fatal oversized request handling, live MLX memory persistence, automatic sparse residency, model storage support, cache-safe large prefill, terminal-only MTP prefill, prompt-cache write-behind, Observatory telemetry, menu memory control, lazy hot-swap routing, replacement-model allocator cleanup, bounded generation queuing, request-memory admission, positional file-read attribution, explicit experimental preparation, one-layer experimental qualification ownership, the staged source-size guard, multi-token prediction status propagation, configuration-root model discovery, and model-serving ownership on 2026-08-07.
